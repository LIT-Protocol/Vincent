---
registry-sdk: minor
contracts-sdk: minor
registry-backend: minor
---

Add user app installation flow and agent account lookup

**app-dashboard:**

- Update `@lit-protocol/vincent-registry-sdk` to use latest published version instead of workspace dependency

**registry-backend:**

- Add `POST /user/:appId/install-app` endpoint to initiate app installation
- Add `POST /user/:appId/complete-installation` endpoint to complete installation with signed data
- Add `POST /user/:appId/agent-account` endpoint to retrieve agent smart account address
- Use `deriveSmartAccountIndex(appId)` for unique per-app smart accounts
- Use Base mainnet for smart account derivation
- Add integration tests for app installation and PKP permissions

**registry-sdk:**

- Remove `enabled` from AppVersion document (status exists in contracts)
- Remove `delegateeAddresses` from App document
- Remove `redirectUris` from App document
- Rename `appUserUrl` to `appUrl` in App document
- Add `POST /user/:appId/install-app` endpoint schema
- Add `POST /user/:appId/complete-installation` endpoint schema
- Add `POST /user/:appId/agent-account` endpoint schema
- Regenerate RTK clients

**contracts-sdk:**

- Add `deriveSmartAccountIndex(appId)` function using `keccak256("vincent_app_id_{appId}")`
- Export function from package index for client-side address derivation
