---
vincent-rest-api: patch
---

`AppClient` contains the app metadata
`CreateApp` is the same, with `appId` and `activeVersion` omitted (`appId` generated server-side)
`AppDef` is just `BaseDocAttributes` merged with `AppClient`