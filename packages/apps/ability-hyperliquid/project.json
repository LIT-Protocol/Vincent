{
  "name": "ability-hyperliquid",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "src",
  "projectType": "library",
  "targets": {
    "clean": {},
    "action:build": {
      "executor": "nx:run-commands",
      "dependsOn": ["ability-sdk:build"],
      "options": {
        "commands": ["pnpm node ./esbuild.config.js"],
        "cwd": "packages/apps/ability-hyperliquid",
        "parallel": false
      }
    },
    "action:deploy": {
      "executor": "nx:run-commands",
      "dependsOn": ["action:build"],
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "packages/apps/ability-hyperliquid"
      }
    },
    "action:only:deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "executor": "@nx/js:tsc",
      "dependsOn": ["action:build"],
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/apps/ability-hyperliquid/dist",
        "main": "packages/apps/ability-hyperliquid/src/index.ts",
        "tsConfig": "packages/apps/ability-hyperliquid/tsconfig.lib.json",
        "assets": [
          "packages/apps/ability-hyperliquid/*.md",
          {
            "glob": "**/*",
            "input": "packages/apps/ability-hyperliquid/src/generated",
            "output": "src/generated"
          }
        ]
      }
    },
    "test": {
      "command": "echo 'Test target disabled for this project - only \"test-e2e\"'"
    },
    "test-e2e": {
      "executor": "@nx/jest:jest",
      "dependsOn": ["e2e-test-utils:build", "action:deploy"],
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/apps/ability-hyperliquid/jest.config.js"
      }
    }
  }
}
